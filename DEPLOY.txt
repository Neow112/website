SAPFIR — FINAL POLISHED BUILD

GitHub Pages deployment:
1. Replace index.html in the repository root.
2. Replace/upload the complete assets/ folder.
3. Replace/upload the complete images/ folder.
4. Upload favicon.svg, robots.txt, sitemap.xml and .nojekyll to the root.
5. .htaccess is not used by GitHub Pages; do not upload it there unless you simply want to keep it as a future Apache-hosting reference.
6. Commit the changes and wait for GitHub Pages deployment to finish.
7. Test in an incognito/private tab.

IMPORTANT
- Do not rename fingerprint CSS/JS files or *.v2.avif files; index.html already points to them.
- Old unversioned *.avif files may be deleted from the repository after this build is deployed; they are no longer referenced.
- Keep JPG files: they are fallback files and gallery.jpg is used for social/SEO previews.

Post-deploy checks:
- desktop + mobile intro
- hero
- RU / KZ / UG
- member dialogs
- gallery + swipe + lightbox
- YouTube click-to-load
- WhatsApp links and number copying
- link preview in Telegram/WhatsApp
