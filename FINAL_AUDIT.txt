SAPFIR — FINAL PRODUCTION CHECK
Date: 2026-09-12

CODE
- Exactly one <!DOCTYPE html>.
- CSS and JavaScript remain external cacheable fingerprint assets.
- JavaScript syntax: OK (node --check).
- Duplicate HTML id values: 0.
- Missing local href/src/srcset resources: 0.
- JavaScript loads with defer.
- Google Noto Sans is retained intentionally to preserve the current typography; preconnect + display=swap are enabled.

IMAGES
- Previous .avif payload issue corrected: all current *.v2.avif files are genuine ISO Media AVIF images.
- AVIF filenames were versioned with .v2 to bypass any browser/CDN cache of the previous files.
- Full-size picture-set totals (11 images, excluding hero main):
  JPEG: 4.36 MiB
  WebP: 3.08 MiB
  AVIF: 1.85 MiB
  AVIF is ~57.6% smaller than the corresponding JPEG set.
- Mobile 900px gallery set:
  WebP: 0.94 MiB
  AVIF: 0.51 MiB
- Hero continues to use main.webp (319 KB) inside the existing SVG mask.
- JPG originals remain for fallback and social crawlers.

SEO / DELIVERY
- Canonical, robots, Open Graph, Twitter Card and Schema.org retained.
- robots.txt and sitemap.xml retained.
- .nojekyll retained for GitHub Pages.
- .htaccess is NOT needed on GitHub Pages; use it only on Apache hosting.

NOTE
A true Lighthouse/Core Web Vitals score must be measured against the deployed public URL. Static package checks cannot reproduce network/server latency or real browser field data.
