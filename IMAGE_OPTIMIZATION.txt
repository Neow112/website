SAPFIR — IMAGE OPTIMIZATION (VERIFIED)

The site now uses genuine AVIF + WebP + JPG fallback through <picture> where appropriate.
The AVIF files are versioned as *.v2.avif to avoid stale browser/CDN cache from an earlier invalid AVIF build.

Full-size responsive picture set (11 images, excluding hero main):
- JPEG total: 4.36 MiB
- WebP total: 3.08 MiB
- AVIF total: 1.85 MiB
- AVIF reduction vs JPEG: ~57.6%

Mobile gallery (5 images at 900px):
- WebP total: 0.94 MiB
- AVIF total: 0.51 MiB

Hero:
- main.jpg: ~481 KB fallback/original
- main.webp: ~319 KB used by the existing SVG-masked hero

Modern browsers select AVIF first, then WebP, then JPG. Only the selected source is downloaded; the browser does not download all formats.
